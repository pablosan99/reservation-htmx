@model Reservation.Frontend.Pages.ReservationForm

<select asp-for="Location" asp-items="Model.PossibleLocations" class="form-control form-select"
        hx-get="@Url.Page("ReservationForm", "Dates")"
        hx-trigger="#reservationDate"
        hx-swap="outerHTML">
</select>
        

