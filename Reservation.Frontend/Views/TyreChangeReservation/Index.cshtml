@model Reservation.Frontend.Models.TyreChangeReservationReservationFormModel
@{
    Layout = "_Layout";
}

<div class="left-panel" id="reservationForm">
    @{ await Html.RenderPartialAsync("_TyreChangeForm", Model); }
</div>
<div class="right-panel">
    @* Htmx out of band     *@
    <div id="location-map">
    </div>
    @* Htmx ws *@
    <div>
        @{ await Html.PartialAsync("_ActionBoard"); }
    </div>
</div>