@model Reservation.Frontend.Models.TyreChangeReservationFormModel

@Html.LabelFor(x => x.WheelType, new {@class = "form-label"})
<select asp-for="WheelType"
        asp-items="Model.WheelTypes"
        class="form-select">
</select>