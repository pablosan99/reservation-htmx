@model Reservation.Frontend.Models.BaseReservationFormModel

@Html.LabelFor(x => x.CarNumber, new {@class = "form-label"})
<input asp-for="CarNumber" type="text" class="form-control size-auto" />
@{
    if (Model.IsPost)
    {
        <span asp-validation-for="CarNumber" class="text-danger"></span>
    }
}